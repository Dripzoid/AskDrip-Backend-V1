services:
  - type: web
    name: askdrip-v1
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn app.main:app \
      -k uvicorn.workers.UvicornWorker \
      -w 1 \
      --timeout 300 \
      -b 0.0.0.0:$PORT